services:
  redis:
    image: redis:latest
    container_name: Redis
    ports:
      - "6379:6379"
    volumes:
      - ${VOLUMEDIR}/redis:/data
    labels:
      - wud.watch=false
    restart: always