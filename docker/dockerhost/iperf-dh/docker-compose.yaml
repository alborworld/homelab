services:
  iperf3-dh:
    image: frauhottelmann/docker-iperf3:latest
    container_name: iPerf3-DH
    network_mode: host
    labels:
      # WUD
      - wud.trigger.include=telegram.1,docker.auto
    restart: always
