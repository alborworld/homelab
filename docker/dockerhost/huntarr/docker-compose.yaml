services:
  huntarr:
    # Newtarr: maintained fork of Huntarr v6.6.3 (upstream abandoned after
    # unfixed auth-bypass/credential-exposure CVEs, Feb 2026)
    image: ghcr.io/elfhosted/newtarr:rolling
    container_name: Huntarr
    volumes:
      - ${VOLUMEDIR}/newtarr:/config
    environment:
      - TZ=Europe/Amsterdam
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    labels:
      - wud.watch=false
      # TRAEFIK
      - traefik.enable=true
      - traefik.http.services.huntarr.loadbalancer.server.port=9705
      - traefik.http.routers.huntarr.rule=Host(`huntarr.${LOCAL_DOMAIN}`)
      - traefik.http.routers.huntarr.entrypoints=websecure
      - traefik.http.routers.huntarr.service=huntarr
      - traefik.http.routers.huntarr.tls=true
      - traefik.http.routers.huntarr.tls.certresolver=cloudflare
      # HOMEPAGE
      - homepage.group=Media Automation
      - homepage.name=Newtarr
      - homepage.icon=huntarr.png
      - homepage.href=https://huntarr.${LOCAL_DOMAIN}
      - homepage.description=Missing and Upgrade Media Hunter (Huntarr fork)
      - homepage.siteMonitor=https://huntarr.${LOCAL_DOMAIN}
    restart: unless-stopped
